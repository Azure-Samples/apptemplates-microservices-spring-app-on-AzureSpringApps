version: v0.1
testName: petclinic-test-scenarios
testPlan: petclinic-test-scenarios.jmx
description: 'PetClinic user scenario tests'
engineInstances: 1
failureCriteria:
  - avg(response_time_ms) > 300
  - percentage(error) > 50